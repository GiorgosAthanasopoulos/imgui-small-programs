{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "command": "${workspaceFolder}/build.sh",
            "args": [
                "${fileBasenameNoExtension}"
            ]
        }
    ]
}